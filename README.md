# GoogleTest C++ test coverage demo

``` bash
vagrant up
vagrant ssh
cd /vagrant/build
cmake ..
make
```

Now open \<project dir\>/build/coverage_report/index.html on the host to view the coverage report.
